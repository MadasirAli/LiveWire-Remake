TextureCube skybox;
SamplerState skyboxSampler;

float4 main(float4 position : SV_Position, float4 texCoords : TEXCOORD) : SV_Target
{
    float4 color = skybox.Sample(skyboxSampler, (float3) texCoords);
    return float4(color.xyz, 1);
}