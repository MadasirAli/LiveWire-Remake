#include "PixelInput_goroud.hlsli"

Texture2D albedo;
SamplerState albedoSampler;

float4 main(PixelInput pixel) : SV_Target
{
    float4 color = albedo.Sample(albedoSampler, pixel.uv);
    return float4(color.xyz, 1) * pixel.color;
}